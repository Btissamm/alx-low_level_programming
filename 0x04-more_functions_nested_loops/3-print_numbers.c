#include "main.h"
/**
 * print_numbers -  prints the numbers, from 0 to 9, followed by a new line
 * 
 * Return: 0
 */
 void print_numbers(void)
{char a;
	from (a = 48; a <= 57; a++)
		_putchar(a);
	return (0);
}
